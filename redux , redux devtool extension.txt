redux , redux devtool extension
also inspect mei redux jaake waha par action click krna h and state pr click kro

store.jsx
cartSlice.jsx
main.jsx

aab cart wala hone ke baad hame
import karna h addToCart function from cartSlice productCards.jsx mei fir
productCards.jsx mei jaake ke changes krne h line 8-12 , 24-28 and 
then navbar.jsx mei bhi changes krne h line 6-7

jo prop ham import kr rhe h productCard.jsx mei i.e {products} woh navbar.jsx mei hamne define kiya h const products = useSelector((state) => state.cart.products);
  // console.log(products);

console.log()par
so now jab console mei jaenge and shop page par addToCart icon ko click krne par hame show hoga array and aagar double click krenge same item pr to item already added aaega

ab shoping cart ka icon 0 => se dynamic banane ke liye ham shopPage.jsx mei jaenge and line no. 76 mei add karenge {product.length}

     lec 90
now again in navbar.jsx
line 8-11 handles toggles of cart , add this toogle function to button on line 39, then write code in line 54 {
      isCartOpen && <Cart Modal/>
    }  
 creating cartModal.jsx in shop folder this will basically make the page blackish on hover of the cart icon and show the cart sidebar section
 then in the cartModal.jsx we will have <orderSummary.jsx/> 
 orderSummary.jsx will be in shop folder and calculate the sum and clear the cart things



backend
after completing the users folder (user.model and routing,jwt part)
we'll go back to the frontend redux folder, feature foldedr, create auth folder here authApi.jsx(here we make all the api )

RTK query


